using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CrowdArea : MonoBehaviour
{
    [SerializeField] private float distanceY;
    [SerializeField] private float distanceX;

    // Update is called once per frame
    void Update()
    {
        
    }
}
